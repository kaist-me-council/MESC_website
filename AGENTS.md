<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## 변경 내역

- 2026-07-05: KAIST 학과 사이트 크롤링으로 교수진 59명·학부과목 64개 시드
- 2026-07-05: navbar 공지사항·커뮤니티 드롭다운 그룹(각 4개 하위 항목) 추가 + 딥링크 연동(`/notices?category=`, `/community?tab=`)
- 2026-07-05: 드롭다운 활성/hover 스타일 절제 + 트랜지션 프로퍼티 명시 폴리시
- 2026-07-05: 강의평(별점+댓글) 기능 — CourseReview 모델·API·과목 상세 UI
- 2026-07-05 모바일 UI 개편 — 메뉴 1열 리스트화 + 터치 타깃·오버플로 전면 점검
- 2026-07-05 모바일 메뉴 카드 그리드 복원(1열 리스트 롤백) + 스크롤 불가 수정 — sticky 헤더에 갇혀 하단 그룹 접근 불가였던 것을 메뉴 패널 `max-h-[calc(100dvh-4rem)] overflow-y-auto`로 해소, 넓은 폭 카드 비대화 방지(반응형 열 수 3→4→6)
- 2026-09-02 개인정보·보안 정리 — `/privacy`·`/terms` 페이지 신설(푸터 링크 404 해소), 주간 백업 AES-256-GCM 암호화(키=sha256(CRON_SECRET), 복호화 `scripts/decrypt-backup.mjs`) + 보유기간 자동 정리(건의 연락처 답변 후 30일·IP 해시 90일·신고 180일), 업로드 이미지 sharp 재인코딩(EXIF/GPS 제거·실제 이미지 검증)·랜덤 suffix, 교수 전화번호 공개 응답 제외, `ANON_SALT` 프로덕션 필수(미설정 시 실패)·후기 비밀번호 scrypt 전환, 관리자 세션 7일, 과비 API 에러 문구·no-store
- 2026-09-02 공개 트래픽 대비 — 홈(60s)·학과정보·구성원(300s) ISR 전환, 공개 GET API 6개에 CDN 캐시 헤더(s-maxage=60), 과비 전역 일일 상한 5천→5만, next/image `sizes` 지정
- 2026-09-03 학생회 구성원 명단 20명·사진 20장 프로덕션 반영(사진은 512px webp로 `/api/upload` 경유 → Blob, Drive 폴더 미참조). 업로드 API 500 수정 — sharp의 linux-x64 바이너리·libvips가 Vercel 함수에 추적되지 않아 라우트 로드 시 실패하던 것을 `next.config.ts` `outputFileTracingIncludes`로 명시(bc4c971). Vercel에서 sharp 쓰는 라우트를 추가하면 같은 항목에 경로를 추가할 것
- 2026-09-04 공지 고정/해제가 수정 후 반영 안 되던 버그 — 원인은 9/2에 넣은 공개 GET API 6개의 CDN 캐시 헤더(`s-maxage=60, swr=300`). DB 저장은 정상이었지만 관리자 목록·/notices가 Vercel 엣지의 옛 응답을 받아 고정 상태가 그대로 보였고, 그 stale 값이 수정 폼에 다시 채워져 재저장 시 되돌아가기도 함. 이 헤더는 쓰기 시 purge할 방법이 없고, POST/PUT이 같이 있는 route.ts는 Next가 ISR로 만들지 못하므로(`hasNonStaticMethods`) 6개 GET 모두 헤더 제거. 공지 POST/PUT/DELETE에 `revalidatePath("/")` 추가(홈 60s ISR 즉시 갱신)
- 2026-09-07 반팔티 1차 구매 확인(1단계) — `/shop/check`(학번+이름 또는 이메일+이름으로 주문 조회 → 받음/못 받음 응답, 마감 9/13 상수 `lib/tshirt.ts`), `/admin/shop`(배부 시트 CSV import·응답 현황·못 받음 집계·CSV). 학번은 `sha256(ANON_SALT:sid:학번)` 해시만 저장(`PriorPurchase.studentIdHash`), 어떤 응답에도 해시 미노출. 파서는 `lib/tshirt-parse.ts`(순수) + `node scripts/tshirt-parse.test.mjs`. 마이그레이션은 `20260907000000_add_prior_purchase` 수기 SQL(로컬 dev.db 는 drift 로 `migrate dev` 불가 → sqlite3 로 직접 적용). 설계: `docs/superpowers/specs/2026-09-07-tshirt-shop-design.md`. 2단계(쇼핑몰)는 1차 마감 후
- 2026-09-07 학생회 이벤트(신청·구매 캠페인) 시스템 — 구글폼 대체. 관리자가 `/admin/campaigns`에서 캠페인(기간·계좌·옵션·재고·구분별 가산)을 만들면 공개 `/apply/[slug]`에서 비회원 신청 → 주문번호·금액·계좌 안내, "내 신청 확인". 신청 목록에서 입금/수령 상태·발주표·CSV·**이메일 복사**(1차 확인 화면에도 추가). 모델 Campaign/CampaignOption/CampaignOrder(`20260907000002_add_campaign`), 헬퍼 `lib/campaign.ts`, e2e `scripts/campaign-api.test.sh <BASE>`. 재고 확인은 트랜잭션 없음(ponytail). 내비 "학과 도우미 > 학생회 이벤트". 설계 `docs/superpowers/specs/2026-09-07-campaign-system-design.md`. 반팔티 2차 구매가 첫 캠페인(관리자 "반팔티 14옵션 채우기" 버튼)
- 2026-09-07 학생회 이벤트 v2 — 반팔티 1차 "구매 확인"을 캠페인의 **수령 확인** 기능으로 흡수(`PriorPurchase`·`/admin/shop`·`/api/shop/*` 제거, `/shop/check`는 `/apply/2026-spring-tshirt/confirm` 리다이렉트). 관리자 프리셋 "상반기 반팔티(수령 확인용) 만들기" → 배부 시트 CSV를 주문으로 적재(`orders/import`, 모드 배부시트/일반) → 구매자가 `/apply/[slug]/confirm`에서 받음/못 받음·항목별 처리 응답. 굿즈 캠페인은 상품형 화면(이미지·색상 칩·사이즈 칩·수량·하단 고정 바). 관리자 일괄 상태 변경(체크박스)·입금 취소·이메일 복사, 신청자 본인 취소(pending만). 설계 `docs/superpowers/specs/2026-09-07-campaign-v2-design.md`, 조언 `2026-09-07-google-form-advisor.md`. **메일 발송 전에 프로덕션에서 프리셋 생성+적재 필수**
- 2026-09-07 학생회 이벤트 v2.1·v3 — 이미지 여러 장(`Campaign.images`, 갤러리 object-contain), 구분별 가산 저장 버그 수정(서버가 JSON 문자열도 수용), 홈 상단 4번째 카드 `/apply`, 입금자명(`depositorName`, 비우면 이름), 관리자 정산 카드, 주문번호 단독 조회·취소·확인, 관리자 인라인 주문 항목 수정(`PUT orders {items}` 서버 재계산), `components/linkify-text.tsx`. 개발 기록 `docs/superpowers/specs/2026-09-07-campaign-dev-log.md`
- 2026-09-07 Codex 검토(`docs/reviews/2026-09-07-campaign-admin-review.md`) 반영 — 관리자 대시보드 13카드를 4묶음(신청·사업 운영/소식·소통/학과·학습 정보/학생회·사이트 설정)으로, 이름 정리(행사 사진·갤러리, 이벤트 신청·구매, 학생회 구성원, 커뮤니티·건의). 캠페인 상세 기본 탭 = 신청 목록, `?tab=` 보존. 주문 적재 "교체" 기본 꺼짐 + 미리보기에 지워질 주문/응답 건수 경고. 못 받음 건 **처리 완료**(`CampaignOrder.resolvedAt`, PUT `{resolved}`, 집계 제외, CSV 열). 정책: 같은 사람 중복 신청 허용
- 2026-09-08 배포 직전 — 개인정보 보유기간 처리를 삭제→**익명화**로(캠페인 마감 180일 후 이름·해시·이메일·전화·입금자명·메모만 비움, 주문 항목·금액·상태 보존). `Campaign.order` 표시 순서(공개·관리자 목록 order asc, createdAt desc). 신청 목록 정렬(최신/오래된/이름/상태/금액). 공개 API 레이트리밋 60/분
- 2026-09-08 배부자 기록 — `CampaignOrder.handedBy`. 수령 처리(단건·일괄) 시 담당자 이름 입력(마지막 값 기억), 배부 시트 적재의 "배부자" 열 자동 반영, 목록 뱃지·CSV 열. 프로덕션 배포 사고: Preview 변수 추가 시 기존 Production DATABASE_URL/TURSO_AUTH_TOKEN 이 Preview 스코프로 옮겨져 새 빌드가 실패 → 프로덕션 스코프에 재추가(토큰 신규 발급)하고 재배포. 환경변수는 "추가"로, 기존 변수 편집 금지
- 2026-09-08 v4 보강(검토 문서 `docs/reviews/2026-09-07-campaign-admin-review.md` 반영) — **데이터 보호**: 주문 적재는 파싱·검증을 먼저 끝내고 문제 행·0건이면 400 거부, 삭제+생성을 한 트랜잭션으로(실패 시 기존 주문·응답 보존), dryRun 은 옵션 미생성. **동시성**: 재고 있는 옵션은 인터랙티브 트랜잭션 안에서 잔여 재계산 후 생성(동시 3요청 → 1건만 201), 본인 취소는 `status=pending` 조건부 갱신(경합 시 409). **본인 변경 보호**: `manageCodeHash`(신청 시 평문 1회 노출, sha256 저장), 취소는 주문번호+관리 코드 필수, 주문번호 단독 상태 변경 전면 차단. **재전송**: `@@unique([campaignId, idempotencyKey])`, 재전송은 기존 주문 200 반환. **maxPerPerson** 동일인 누적 합산. **CSV** 수식 주입 중화(`= + - @ TAB CR` → `'` 접두). **환불 완료** `refundedAt` 토글·뱃지·정산 카드(환불 대기 건수·금액)·CSV 열. **관리자 정보구조**: `lib/admin-menu.ts` 단일 정의, protected layout 공통 헤더+4그룹 내비+브레드크럼, 캠페인 상세 4탭(신청 목록/수령 확인/설정/데이터 가져오기, `?tab=`), 필터 변경 시 선택 초기화, 설정 저장 함수형 갱신. **공개 화면**: 통신 실패·429·4xx·5xx 구분 표시(빈 목록과 혼동 금지), 실패 시 입력 보존, 완료 카드에 관리 코드. 프로덕션 `PriorPurchase` 는 테이블 자체가 없어 데이터 이전 이슈 없음(검토 항목 해소)
- 2026-09-08 사이트 전반 보강(계획서 `docs/reviews/2026-09-08-site-wide-enhancement-plan.md` 반영, 설계 `docs/superpowers/specs/2026-09-08-site-wide-enhancement-design.md`) — **과비 확인**: GET(?id=학번) → POST 본문, 기존 GET 은 410. 학번이 URL·서버 로그에 남지 않음. **로그인**: `authorize` 안에서 계정(10회/10분)·IP(20회/10분) 제한 + 실패 지연, 성공 시 초기화. 라우트 레벨 옛 제한은 제거(성공까지 세고 초기화가 없어 관리자가 스스로 잠기던 버그). **감사 로그** `AdminAudit` — 주문 상태·항목 수정·적재·캠페인/공지 삭제. 개인정보 원문 미저장, 180일 후 크론 삭제. **조회수** `ContentView`(방문자 토큰 쿠키 1개 + 유니크 표, 24시간 중복 제거, 관리자·봇 제외, 존재 여부 비노출) + `Notice/Event/Post.viewCount`, 90일 후 크론 삭제. **계획서의 "콘텐츠별 서명 쿠키" 방식은 채택하지 않음**(쿠키 수·헤더 한계, 서버 검증 불가). **좋아요** `PostLike` 유니크 토글 + `Post.likeCount`. **공개 화면** 4상태 통일(`lib/fetch-state.ts`·`components/load-error.tsx`) — 통신 실패를 빈 목록으로 표시하던 문제 해소. **관리자 오늘 할 일** 4카드(집계 실패 시 0 대신 "-"). **사이트맵** `/apply`·`/library`·`/privacy`·`/terms`·공개 캠페인 추가. **푸터** 사이트 설정을 서버에서 읽어 props 전달(`lib/site-data.ts`, 300초 캐시) — 페이지마다 API 2회 호출하던 것 제거. e2e `scripts/site-api.test.sh` (계정 잠금 블록은 실제 계정을 10분 잠그므로 반드시 마지막)
- 2026-09-08 비공개 캠페인 미리보기 — 관리자 세션이면 `GET /api/campaigns/[slug]` 가 비공개 캠페인도 `preview:true` 로 반환하고 목록에도 포함. 비로그인은 그대로 404, 공개 목록에도 미노출. `/apply/[slug]` 상단에 미리보기 배너·비공개 뱃지, 신청 버튼 비활성(서버도 차단). 관리자 상세의 링크 문구는 비공개일 때 "미리보기 (비공개 — 관리자만 보임)". e2e v5 블록
- 2026-09-08 공지 첨부·PWA/푸시·아이콘·마스코트 — **첨부파일**: `NoticeAttachment` + `/api/admin/upload-file`(확장자 allow-list + 실제 시그니처 검사, 20MB, 랜덤 키 저장, 원본명은 메타데이터만), 공지 POST/PUT 에 `attachments`(PUT 전체 교체), 삭제 시 blob best-effort 삭제. 첨부 url 은 우리 Blob 호스트만 허용. **웹 푸시**: `PushSubscription` + `public/sw.js`(푸시 전용, 페이지 캐시 없음) + `lib/push.ts`(410/404 정리, failCount 5 초과 삭제) + `/api/push/subscribe` + `/api/admin/push/test` + `/api/notices/[id]/notify`. 공지 작성 시 `notify:true` 일 때만 발송(Vercel 특성상 await 후 응답). **아이폰은 홈 화면에 추가한 PWA 에서만 알림 수신** — UI 가 iOS 미설치 상태를 감지해 설치 안내로 분기. VAPID 키는 프로덕션·프리뷰 환경변수에 설정됨(`NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`). **아이콘**: `scripts/make-icons.mjs`(sharp)로 기어 마크 생성 — `app/icon.svg`·`favicon.ico`(직접 만든 ICO 컨테이너)·`apple-icon.png`·192/512/512-maskable·`opengraph-image.png`. 32px 는 기어만, 큰 사이즈는 기어+ME. **마스코트**: `Mascot` 모델 + `/api/mascots`, 관리자 사이트 설정에 마스코트 탭(사진 업로드·공개 토글), 학부 소개에 마스코트 탭(항목이 있을 때만 노출)
- 2026-09-08 첨부 30MB — **Vercel 서버리스 함수는 요청 본문이 4.5MB 로 제한**된다(초과 시 함수 실행 전 413, 실측 확인). 기존 20MB 표기는 프로덕션에서 동작하지 않는 값이었다. 브라우저 → Blob 직접 업로드(`@vercel/blob/client` `upload()`)로 전환: `/api/admin/upload-file/token`(handleUpload — 인증·경로 `notices/<uuid>.<ext>`·형식·30MB 제한) + `/api/admin/upload-file/verify`(업로드된 파일 앞 512B 를 Range 로 받아 시그니처 확인, 불일치 시 blob 삭제 후 400). 규칙은 `lib/upload-rules.ts` 공용. 구 `/api/admin/upload-file` 삭제. **이미지 업로드(`/api/upload`)는 sharp 로 EXIF 를 지워야 해서 서버를 거쳐야 하므로 상한을 5MB→4MB 로 낮춤**(5MB 는 플랫폼 한도를 넘어 실패하던 값)
- 2026-09-08 공지 첨부 구글 드라이브 저장 — 학생회 공식 드라이브에 저장하고 **드라이브 링크는 노출하지 않는다**. 업로드: `/api/admin/upload-file/drive-session`(인증·확장자·30MB 검증 후 resumable 세션 URI 발급) → 브라우저가 구글로 직접 PUT(4.5MB 함수 한도 회피) → `/api/admin/upload-file/drive-verify`(존재·크기·앞 512B 시그니처 확인, 불일치 시 드라이브 파일 삭제). 다운로드: `/api/notices/attachments/[id]` 가 학생회 토큰으로 받아 스트리밍(RFC5987 한글 파일명, 공개 공유 안 함). `NoticeAttachment.driveFileId` 추가(기존 Blob 행은 그대로). **드라이브 미연결·직접 PUT 실패(CORS 등) 시 자동으로 Blob 으로 폴백**하고 관리자 화면에 저장 위치를 표시. 폴더는 `ensurePrivateSubfolder`(기존 `ensureSubfolder` 는 makePublic 을 호출하므로 첨부에는 쓰지 않음). 프리뷰 DB 에는 DriveAuth 가 없어 항상 Blob 폴백 — 실제 드라이브 경로는 프로덕션에서만 검증 가능
- 2026-09-08 **CSP connect-src 게이트 (재발 주의)** — 공지 첨부를 브라우저에서 구글 드라이브로 직접 올리는데 `connect-src` 에 googleapis 가 없어 모든 업로드가 "Failed to fetch" 로 실패했다. 서버 경로(세션 발급·PUT·verify)와 구글의 CORS 응답은 모두 정상이었고 브라우저만 막힌 것. **외부 도메인으로 fetch/XHR 을 보내는 기능을 추가하면 `next.config.ts` 의 connect-src 를 먼저 확인할 것.** 이미지(img-src)·프레임(frame-src)에 이어 세 번째 같은 유형의 사고
- 2026-09-08 푸시 구독 실패 — 클라이언트는 `{subscription: sub.toJSON(), userAgent}` 로 보내는데 서버는 최상위 `endpoint`/`keys` 만 읽어 항상 400. 서버가 두 형태를 모두 받도록 수정. 더불어 `NEXT_PUBLIC_VAPID_PUBLIC_KEY` 는 빌드 시점에 코드로 박히므로 나중에 환경변수를 넣으면 재빌드 전까지 반영되지 않는다 → `GET /api/push/key` 런타임 조회 폴백 추가
- 2026-09-08 **첨부 업로드가 전부 실패하던 진짜 원인 — CSP connect-src 두 곳** ① 구글 드라이브 직접 업로드용 `googleapis` 누락 ② **Blob 클라이언트 업로드가 쓰는 `blob.vercel-storage.com` 누락**(`*.public.blob.vercel-storage.com` 만 열려 있었음 — public 접두사 없는 API 호스트가 별개). 두 경로가 모두 막혀 67KB 파일도 "Failed to fetch". 크롬으로 재현·확인. **드라이브 직접 PUT 은 CSP 를 열어도 브라우저 preflight 가 통과하지 못했다**(curl 로 같은 사전 요청을 보내면 allow 헤더 정상, 서버→구글 PUT 도 정상). 그래서 최종 구조는 **브라우저→Blob 업로드 → 서버가 Blob 을 받아 드라이브로 옮기고 Blob 삭제**(`/api/admin/upload-file/to-drive`, maxDuration 60). 서버는 파일을 요청 본문으로 받지 않고 URL 에서 받아오므로 4.5MB 한도와 무관. 이동 실패 시 Blob 첨부 유지
- 2026-09-08 첨부 업로드 UI 가 "업로드 중" 에서 멈추던 문제 — 브라우저→구글 직접 PUT 단계를 완전히 제거(사전 요청이 브라우저에서 통과하지 못해 매번 실패·지연만 유발). 최종 흐름은 **브라우저→Blob 업로드 → 서버가 드라이브로 이동 → Blob 삭제**. 각 단계에 시간 제한(확인 30s·이동 90s)과 전체 상한 5분, 단계별 진행 문구를 넣어 어떤 경우에도 멈춘 채 남지 않게 함. `drive-session`·`drive-verify` 라우트 삭제
- 2026-09-08 회귀 9건 수정(지시서 `docs/reviews/2026-09-08-site-regression-fix-instructions.md`) — **U1** `ensurePrivateSubfolder` 인자 순서가 뒤바뀌어 있던 것(선언 `(token,parentId,name)` / 호출 `(token,"공지 첨부",parentFolderId)`) → 객체 인자 `{accessToken,parentId,name}` 로 변경해 같은 타입 혼동 차단. **U2** 공지 저장이 `res.ok` 확인 없이 `resetForm()` 하던 것 → 성공 확인 후에만 초기화, 실패 시 입력·첨부 보존, "저장 완료·목록 갱신 실패" 구분, 응답 유실 시 자동 재등록 금지. **U3** PUT 이 첨부를 전부 지우고 다시 만들어 다운로드 URL 이 매번 깨지던 것 → id 보존 diff(키 없음=변경없음, `[]`=전체삭제, 타 공지 id 거절, 저장위치·URL 은 서버에서 읽음), 본문+첨부 트랜잭션, 실파일 정리는 커밋 후·공유 참조 확인 후. **U4** 업로드가 폼 세션에 묶이지 않아 다른 공지에 파일이 붙던 것 → 세션 ID 가드 + 재진입 가드 + AbortController 실제 취소. **P1/P4** 브라우저 구독과 서버 등록을 분리하고 `components/push-provider.tsx` 로 두 토글이 상태 공유(서버 확인 전 `on` 표시 금지, 마운트 시 자동 구독 금지). **P2** `PushResult.status` 로 미설정/조회실패/구독자0/부분실패/완료 구분, 공지 저장 응답의 `notified` 를 별도 필드로. **P3** `sw.js` 가 관계없는 탭을 `navigate` 하던 것 제거 — 정확한 URL 탭만 focus, 없으면 새 창, same-origin 검사. **V1** 방문자 토큰 발급을 `/api/visitor` 한 곳으로 모으고(다른 라우트는 발급 불가) 클라이언트는 `visitorReady()` 이후 요청, 좋아요는 `PUT {liked}` 목표 상태 방식으로 재시도 수렴. 남는 한계: Web Locks 미지원 브라우저의 두 탭 동시 최초 진입
- 2026-09-08 **첨부 업로드가 프로덕션에서만 안 되던 근본 원인 — 번들러 차이** `@vercel/blob@2.3.1` 의 브라우저 동작은 package.json 의 레거시 `browser` 필드로 `undici`→`undici-browser.js` 를 바꿔치기하는 데 의존한다. **프로덕션은 Turbopack, 내 로컬 검증은 `next build --webpack`** 이라 로컬만 통과하고 프로덕션에서는 `upload()` 가 토큰을 받은 뒤 요청조차 보내지 못하고 영원히 대기했다(로그인된 브라우저에서 재현·확인). 교훈: **로컬 검증 빌드를 프로덕션과 같은 번들러로 맞추거나, 브라우저에서 서드파티 업로드 라이브러리를 쓰지 말 것.** 조치 — 브라우저에서 `@vercel/blob/client` 제거, 파일을 3MB 청크로 **우리 서버에 보내고 서버가 드라이브 resumable 세션에 이어 붙이는** 방식으로 교체(`drive-begin`·`drive-chunk`, HMAC 서명 uploadId, 308 Range 재동기화, 완료 시 크기·시그니처 재검증). 드라이브 미연결 시 4MB 이하만 서버 경유 Blob 폴백
- 2026-09-08 알림이 안 오던 이유 — 서버는 `notify:true`·`/api/notices/[id]/notify`·`/api/admin/push/test` 를 모두 갖췄지만 **관리자 화면에 이를 누를 수단이 아예 없었다**(구독은 4건 등록돼 있었고 `lastSuccessAt` 이 전부 NULL = 발송 시도 자체가 없었음). 공지 작성 폼에 "알림 보내기" 체크박스, 목록 행마다 "알림" 버튼(기존 공지 재발송), 목록 위에 "알림 시험 발송" 버튼 추가. 발송 결과는 `sent/partial/no_subscribers/not_configured/lookup_failed` 를 사람이 읽는 문장으로 표시하고, 저장 성공과 알림 결과를 한 줄에 함께 안내(알림 실패가 저장 실패로 보이지 않게)
- 2026-09-08 사용 매뉴얼·카드뉴스 — `docs/manual/학생용-사용법.md`(학생), `docs/manual/관리자-운영매뉴얼.md`(관리자), 프로덕션 실화면 캡처 11장 `docs/manual/images/`. 학생용은 인스타 1:1 카드뉴스 8장 `docs/manual/cardnews/card-01..08.png`(1080×1080)로도 발행. **카드는 `scripts/make-cardnews.mjs`(sharp SVG→PNG)로 생성** — 이미지 직접 편집 금지, 스크립트의 `cards` 배열 문구를 고치고 재실행. 헤드리스 크롬(`--screenshot`)은 이 환경에서 응답 없이 멈춰 폐기했고, SVG 는 자동 줄바꿈이 없어 줄을 데이터에서 직접 나눈다(본문 한 줄 20자 안쪽). 이모지는 렌더되지 않으므로 문구에 넣지 않음
- 2026-09-08 검토 지적 4건 수정 — **① 첨부 청크 건너뜀(데이터 손상)**: 구글이 308 에 Range 를 안 주면 규약상 "0바이트 수신"인데 서버가 `start + 보낸양`으로 낙관해 돌려주고, 클라이언트도 `received > start` 일 때만 반영해 그 외에는 보낸 끝으로 전진했다 → 두 지점 모두 **서버가 확인한 위치만 신뢰**하도록 수정(`nextChunkStart` in `lib/upload-rules.ts` + `scripts/upload-offset.test.mjs`), 진행이 없으면 같은 구간을 재전송하고 3회 연속이면 중단. **② 알림 시험 발송이 전체 발송**: `/api/admin/push/test` 가 `sendPushToAll` 을 불러 학생 전원에게 나갔다 → 클라이언트가 자기 구독 endpoint 를 보내고 서버는 `sendPushToEndpoint` 로 **그 기기에만** 발송(`status=not_subscribed` 추가, 버튼 라벨 "알림 시험 발송 (내 기기)", endpoint 는 감사 로그에 미기록). **③ 취소 후에도 다음 전송 시작**: `fetchWithTimeout` 이 이미 aborted 인 signal 을 검사하지 않아(addEventListener 는 과거 abort 를 재발화하지 않음) 파일 사이 간격에서 취소가 무시됐다 → 진입 시 `outer.aborted` 검사 + 파일 루프 매 회차 취소·세션 검사, 그리고 늦게 끝난 작업이 `uploadingRef` 를 풀어 새 업로드의 가드까지 해제하던 것을 세션 일치 시에만 풀도록. **④ 알림 버튼이 "처리 중"에 고착**: `serviceWorker.register`/`ready`/`subscribe`/서버 등록에 상한이 없었다 → 각각 15/15/20/15초 상한(`withTimeout`, `AbortSignal.timeout`), 초과 시 `error="timeout"` 과 안내 문구(`push.timeout`)로 재시도 유도. init·disable·retry 경로에도 동일 적용
- 2026-09-08 사용 매뉴얼 확장·화면 문구 정정 — 매뉴얼을 요약본에서 레퍼런스로 재작성(`docs/manual/`): 학생용 12장(메뉴 지도·공지·알림·이벤트 전 과정·과비·캘린더·강의·평면도·커뮤니티·개인정보·FAQ), 관리자 12장(대시보드 4카드 정의·캠페인 4탭·공지 첨부와 알림 3경로·행사/드라이브·모더레이션·팝업·학과 정보·사이트 설정·**되돌릴 수 없는 작업 목록**·개인정보·문제 해결·인수인계 체크리스트), `부록A-규칙과-한도.md`(업로드 한도·보유기간·레이트리밋·알림 조건·캐시 지연·주문 상태 기계·환경변수), `부록B-오류-문구-사전.md`(화면 문구 → 원인 → 조치), `README.md` 색인. 조사는 공개/관리자/정책 3개 에이전트 병렬. **코드 정정 3건**: `fill()` 이 `{name}` 을 치환하지 않아 재고 부족 안내가 학생 화면에 "{name} 옵션의 남은 수량이…" 로 그대로 노출되던 것, 행사 "재동기화" 안내가 삭제도 반영된다고 잘못 안내하던 것(실제로는 추가만), 마스코트·캠페인 이미지 안내의 "5MB 이하"(실제 `/api/upload` 한도는 4MB)
- 2026-09-08 매뉴얼 PDF — `scripts/make-manual-pdf.mjs` 가 `docs/manual/*.md` 를 인쇄용으로 조판해 `docs/manual/pdf/` 에 찍는다(학생용 27쪽 / 관리자+부록A·B 45쪽). 의존성 없이 마크다운을 직접 HTML 로 바꾸고(표·인용·목록·이미지·코드) 헤드리스 크롬으로 출력. **크롬은 `--user-data-dir` 로 별도 프로필을 주지 않으면 사용자의 크롬과 충돌해 응답 없이 멈춘다**(이전 라운드 스크린샷 시도가 두 번 멈춘 원인). 쪽 번호가 있는 꼬리말은 CLI `--print-to-pdf` 로는 못 넣어 DevTools 프로토콜(`Page.printToPDF`, Node 내장 WebSocket)로 붙였고, **연결은 반드시 `/json/list` 의 `type:"page"` 대상으로** 해야 한다(브라우저 수준 소켓에는 Page 도메인이 없어 'Page.enable' wasn't found). 표지 h1 은 본문 h1 규칙(남색 글자·페이지 넘김)을 물려받아 남색 바탕에서 안 보였으므로 색·테두리·page-break 를 모두 재선언
- 2026-09-08 프리뷰 배포 실패 — **프리뷰 스코프에 `DATABASE_URL`/`TURSO_AUTH_TOKEN` 이 없어** `libsql-migrate` 가 스킵되고 빈 SQLite 로 빌드가 진행돼, 서버 컴포넌트 프리렌더가 `SQLITE_ERROR: no such table: main.Building` 으로 던지면서 빌드 전체가 중단됐다(프로덕션은 변수가 있어 정상). 8/25 사고 때 프로덕션 스코프에만 재추가한 것이 원인. 조치: `lib/safe-query.ts` 로 **프리렌더 중 DB 조회 실패를 빈 값으로 흡수**(홈·학부 소개·학과 안내·과목 상세). 데이터가 없어도 빌드가 끝나고, ISR 이라 다음 갱신에 정상 데이터로 채워진다. 프로덕션 빌드 중 DB 가 잠깐 흔들려도 배포가 막히지 않는다. 재현·검증: `DATABASE_URL="file:/tmp/빈.db" npm run build` → 이전에는 중단, 지금은 80/80 통과. 프리뷰에서 실제 데이터를 보려면 프리뷰 스코프에 별도 DB 변수를 넣어야 한다(프로덕션 DB 를 그대로 쓰면 프리뷰 코드가 실데이터를 건드린다)
- 2026-09-08 카드뉴스 v2 — 8장 → 11장으로 다시 만들고 확장된 학생용 매뉴얼과 내용을 맞췄다(무엇이 있나 / 알림 / 아이폰 설치 / 신청 / 관리 코드 / 조회 / 수령 확인 / 과비 / 그 밖에 / 마무리). 레이아웃을 부품화(번호 배지+분류 라벨 머리, 반투명 패널, 번호 단계·점 목록, 하단 강조 띠)하고 **강조 띠 위치를 패널 높이에서 계산**하도록 바꿨다 — 좌표를 손으로 적었더니 항목이 3개인 카드에서 목록과 겹쳤다
- 2026-09-08 프리뷰 배포 중단 — `vercel.json` 의 `ignoreCommand: [ "$VERCEL_ENV" != "production" ]`. **Ignored Build Step 은 종료코드 0 이면 빌드를 건너뛴다** — 프리뷰는 0(건너뜀), 프로덕션은 1(빌드 진행). 프리뷰 스코프에 DB 변수가 없어 실데이터를 못 보는 상태였고 실패 알림만 쌓여서 껐다. 되살리려면 이 한 줄을 지우고, **프리뷰 스코프에 DB 변수를 따로 넣을 것**(프로덕션 DB 를 그대로 쓰면 프리뷰 코드가 실데이터를 건드린다). 대시보드 설정이 아니라 저장소에 두었으므로 되돌리기는 이 줄 삭제로 끝난다
